# P0 placeholder Dockerfile.
# Replace once the Next.js app exists.

FROM node:22-alpine AS base
WORKDIR /app

RUN corepack enable

COPY . .

CMD ["sh", "-c", "echo 'Seshat web placeholder. Implement apps/web before running.' && sleep infinity"]
